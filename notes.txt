# Building 

rm -r ./voronotalt.egg-info
rm ./dist/*.tar.gz
rm ./dist/*.whl

~/my-venv/bin/python3 -m build
rm ./dist/*.whl

# Uploading only source package
#~/my-venv/bin/twine upload dist/voronotalt-0.9.5.post4.tar.gz --verbose

# Unzipping wheel archives
cd ./dist
ls cibw-wheels-*.zip | xargs -L 1 unzip
cd

# Uploading all packages
~/my-venv/bin/twine upload dist/*.tar.gz dist/*.whl --verbose

